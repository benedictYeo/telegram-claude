---
phase: 01-infrastructure-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-04
files_modified: []
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-06
  - INFRA-07
  - INFRA-08
  - INFRA-09
  - INFRA-10
  - INFRA-11
must_haves:
  truths:
    - "Server responds to Telegram messages on Mac mini"
    - "PM2 restarts the server after a crash"
    - "Mac mini stays awake with display off overnight"
    - "Tampered webhook signature returns 401"
    - "/health returns 200 from public Tailscale Funnel URL"
    - "/ping command replies pong in Telegram"
    - "Non-text message receives 'Text only for now.' reply"
    - "Server survives a reboot (PM2 launchd)"
  artifacts: []
  key_links: []
---

<objective>
Smoke test the full deployment on Mac mini. Verify all Phase 1 success criteria from ROADMAP.md: authenticated webhook processing, PM2 crash recovery, sleep prevention, Tailscale Funnel public access, slash commands, message chunking, and reboot survival.

Purpose: Human verification that the entire Phase 1 stack works end-to-end on the real Mac mini hardware. This is the gate before Phase 1 is marked complete.
Output: Verified deployment. Phase 1 success criteria confirmed.
</objective>

<execution_context>
@/Users/benedictyeo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benedictyeo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Deploy and verify on Mac mini</name>
  <what-built>
Complete Phase 1 infrastructure: Hono HTTP server with Telegram webhook auth, command routing (/ping, /help), ack-and-defer pattern, typing indicator, message chunking, error handling. PM2 process management with caffeinate sleep prevention. Tailscale Funnel for public HTTPS.
  </what-built>
  <how-to-verify>
**Pre-deploy (on Mac mini):**
1. Clone/pull the repo on Mac mini
2. Run `./scripts/setup.sh` (follow prompts for sudo, .env creation, Tailscale approval)
3. Verify `pm2 list` shows both `tg-claude` (online) and `tg-claude-caffeinate` (online)

**Functional tests (in Telegram):**
4. Send `/ping` to the bot -- expect reply: "pong"
5. Send `/help` to the bot -- expect reply with command list showing /ping and /help
6. Send "hello" (plain text) -- expect reply: "Received. AI coming soon."
7. Send a photo or sticker -- expect reply: "Text only for now."

**Auth tests (via curl from another machine):**
8. `curl https://YOUR-FUNNEL-URL/health` -- expect: "ok" (200)
9. `curl -X POST https://YOUR-FUNNEL-URL/webhook -H "Content-Type: application/json" -d '{}'` -- expect: 401 (no secret header)

**Resilience tests:**
10. Kill the server: `pm2 stop tg-claude && pm2 start tg-claude` -- confirm bot responds to /ping after restart
11. Simulate crash: `pm2 delete tg-claude && pm2 start ecosystem.config.cjs` -- confirm bot responds
12. Check sleep prevention: `pmset -g assertions` should show caffeinate preventing sleep
13. Leave Mac mini overnight with display off -- check in the morning that /ping still responds

**Reboot test (CRITICAL):**
14. Reboot Mac mini: `sudo reboot`
15. After reboot, verify `pm2 list` shows both processes online
16. Send /ping in Telegram -- expect: "pong"
17. Verify Tailscale Funnel is active: `tailscale funnel status`
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe which tests failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet -> Tailscale Funnel | TLS-terminated public HTTPS endpoint |
| Funnel -> localhost:3000 | Internal HTTP between Tailscale and Hono |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Denial of Service | Mac mini sleep | mitigate | pmset + caffeinate belt-and-suspenders verified by overnight test |
| T-01-17 | Availability | PM2 reboot survival | mitigate | pm2 startup + pm2 save + launchd verified by reboot test |
</threat_model>

<verification>
All ROADMAP.md Phase 1 Success Criteria verified:
1. Ben's Telegram message gets 200 response within 1s; other accounts silently dropped
2. Mac mini left overnight, server still responding in the morning
3. Tampered webhook signature returns 401; /health returns 200 with no sensitive info
4. Slash commands routed before AI dispatch; long messages chunked (if tested)
5. Server auto-restarts after crash (PM2); survives reboot (launchd)
</verification>

<success_criteria>
All 17 verification steps pass on the real Mac mini. Phase 1 is complete. Ready for Phase 2: Conversation Core.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-05-SUMMARY.md`
</output>
